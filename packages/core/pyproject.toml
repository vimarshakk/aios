[project]
name = "aios-core"
version = "0.3.0"
description = "AIOS — namespace package root"
requires-python = ">=3.11,<3.14"
license = "Apache-2.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aios"]
