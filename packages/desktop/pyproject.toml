[project]
name = "aios-desktop"
version = "0.3.0"
description = "AIOS Desktop Integration — OS-level clipboard, notifications, screen capture, file dialogs, and system info"
requires-python = ">=3.11,<3.14"
license = "Apache-2.0"
dependencies = [
    "aios-core",
]

[project.optional-dependencies]
dev = ["pytest>=8.3", "pytest-asyncio>=0.24"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aios"]
