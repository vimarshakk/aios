[project]
name = "aios-ui"
version = "0.1.0"
description = "AIOS UI — shared design system and component library"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aios/ui"]
